using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Placeholder MonoBehaviour for a future stress minimisation controller.
/// </summary>
public class StressMinimisation : MonoBehaviour
{
    // Start is called before the first frame update
    void Start() { }

    // Update is called once per frame
    void Update() { }
}
