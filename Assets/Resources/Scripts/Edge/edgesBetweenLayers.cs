using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Placeholder MonoBehaviour for features related to inter-layer edges in an MLN.
/// Extend this component with logic to filter, style or analyse edges that span layers.
/// </summary>
public class edgesBetweenLayers : MonoBehaviour
{
    /// <summary>Reference to the active multilayer network.</summary>
    public MLN mln;

    // Start is called before the first frame update
    void Start()
    {
        // Reserved for future initialisation.
    }

    // Update is called once per frame
    void Update()
    {
        // Reserved for future per-frame logic (e.g., dynamic visibility, highlighting).
    }
}
